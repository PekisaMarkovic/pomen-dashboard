import { FormProvider, useForm } from 'react-hook-form'

const NewCertificatePage = () => {
  const methods = useForm({})

  return <FormProvider {...methods}>TODO</FormProvider>
}

export default NewCertificatePage
